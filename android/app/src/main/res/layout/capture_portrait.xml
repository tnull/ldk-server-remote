<?xml version="1.0" encoding="utf-8"?>
<!--
  Custom layout for PortraitCaptureActivity. Mirrors zxing-android-embedded's default
  zxing_capture.xml but adds a close button so users can abort the scan without hunting
  for the system back gesture.

  The DecoratedBarcodeView id must stay `@id/zxing_barcode_scanner` — the parent
  CaptureActivity looks it up by that id.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <com.journeyapps.barcodescanner.DecoratedBarcodeView
        android:id="@id/zxing_barcode_scanner"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <ImageButton
        android:id="@+id/portrait_capture_close_button"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="top|start"
        android:layout_marginStart="12dp"
        android:layout_marginTop="12dp"
        android:background="@android:color/transparent"
        android:contentDescription="@string/scanner_close_description"
        android:padding="12dp"
        android:src="@android:drawable/ic_menu_close_clear_cancel"
        android:tint="@android:color/white" />

</FrameLayout>

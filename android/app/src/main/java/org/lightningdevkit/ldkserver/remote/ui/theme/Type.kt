package org.lightningdevkit.ldkserver.remote.ui.theme

import androidx.compose.material3.Typography

// Default Material 3 typography. Kept as a named constant so later we can swap in a
// monospace font for amounts / addresses without touching Theme.kt.
val AppTypography = Typography()
